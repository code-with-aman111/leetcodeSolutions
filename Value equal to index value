class Solution{
public:
	vector<int> valueEqualToIndex(int arr[], int n) {
	    vector<int>t;
	    for(int i=0;i<n;i++){
	        if(arr[i]==i+1){
	            t.push_back(arr[i]);
	           
	        }
	    }
	    return t;
	}
};
