#include <iostream>
#include <stack>
using namespace std;
int main() {
   stack<int>s;
   s.push(2);
   s.push(3);
   s.pop();
  // s.top();
   if(s.empty()){
       cout<<"it is empty"<<endl;
   }
   else{
      cout<<" the val is----"<<s.top();
     }
    return 0;
}
